version: "3.8"
services:
  db:
    image: postgres:15
    container_name: monitor-db
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: monitor_db
    ports:
      - "5432:5432"